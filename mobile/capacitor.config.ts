import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.garage.simulation',
  appName: 'Garage Simulation',
  webDir: 'dist'
};

export default config;
